---
import BaseLayout from '../layouts/BaseLayout.astro';
import Icon from '../components/Icon.astro';
import DeliveryStatus from '../components/DeliveryStatus.astro';
import ArchMap from '../components/ArchMap.astro';
import PlatformValues from '../components/PlatformValues.astro';
import UsageCases from '../components/UsageCases.astro';
import { poesisUsage } from '../data/usage.ts';
import ThinkBottleneckDiagram from '../components/ThinkBottleneckDiagram.astro';
import ThinkTherapyDiagram from '../components/ThinkTherapyDiagram.astro';
import { profilesForDomain } from '../data/profiles.ts';
import { services } from '../data/services.ts';
import { siteNavigation } from '../data/site-navigation.ts';

const itProfiles = profilesForDomain('it');
const partnerships = siteNavigation.find((group) => group.label === 'Partnerships')!.sections.flatMap((section) => section.items);
---
<BaseLayout title="The Organizational Intelligence Platform" pageType="Home">
  <section class="hero home-hero hero--split">
    <div class="container">
      <p class="eyebrow">Toward the autonomous enterprise</p>
      <h1>The Organizational Intelligence Platform.</h1>
      <p class="lead">Poesis brings people and AI around shared <em>definitions</em> of the organization: accepted intent, typed relationships and reviewable evidence. Build on available foundations, with reasoning, appraisal and generation workflows on the roadmap.</p>
      <div class="hero-promise">
        <p>Build a governed organizational digital twin, connecting definitions to evidence, human decisions and bounded AI workflows. Start with a defined scope and expand observation coverage and authorized action along the road toward the autonomous enterprise.</p>
      </div>
      <div class="hero-cta">
        <a class="btn btn-primary" href="/contact" data-demo-cta="homepage-hero-pilot"><Icon name="play" /> Discuss a Pilot</a>
      </div>
      <p class="hero-note">Human-governed definitions that people and AI can act on.</p>
      <nav class="hero-sections" aria-label="On this page">
        <a href="#pains"><span>Pain points</span></a>
        <a href="#affordances"><span>Affordances</span></a>
        <a href="#values"><span>Values</span></a>
        <a href="#postures"><span>Deployment &amp; integration postures</span></a>
        <a href="#usage"><span>Use cases</span></a>
        <a href="#personas"><span>Actor types</span></a>
        <a href="#pilot"><span>Start with one workflow</span></a>
        <a href="#portfolio"><span>The Poesis platform</span></a>
        <a href="#services"><span>Services</span></a>
        <a href="#partnerships"><span>Partnerships</span></a>
      </nav>
    </div>
  </section>

  <section id="pains" class="section section--soft home-inversion">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Pain points</p>
        <h2>Drift at machine speed, or align at human speed.</h2>
        <p class="lead">This is the central tension of industrial GenAI adoption. GenAI accelerates the production of artifacts, but not the production of definition and governance required to viably deliver these artifacts.</p>
        <p class="lead">The scarce resource is now more than ever the THINK: the AI and human capacity to understand, define, plan, validate, and take responsibility for what has been created.</p>
      </div>
      <ThinkBottleneckDiagram />
    </div>
  </section>

  <section id="affordances" class="section home-therapy">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Affordances</p>
        <h2>Poesis Gene Therapy</h2>
        <p class="lead">Available foundations and planned workflows for shared context, reviewed changes and bounded action. Each affordance states its scope.</p>
      </div>
      <ThinkTherapyDiagram />
    </div>
  </section>

  <section id="values" class="section section--soft home-values">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Values</p>
        <h2>The organization becomes intelligible.</h2>
        <p class="lead">Put shared definitions to work in decisions, delegated workflows and evidence review. Each value is a company commitment, marked Implemented or Planned for its stated scope.</p>
      </div>
      <PlatformValues />
    </div>
  </section>

  <div id="usage"><UsageCases cases={poesisUsage.useCases.filter((useCase) => ['read-governance-model', 'reconcile-code-evidence', 'review-scoped-appraisal', 'deliver-reviewed-artifact'].includes(useCase.id))} title="From a concrete need to a reviewed outcome" /></div>

  <section id="postures" class="section home-postures">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Deployment &amp; integration postures</p>
        <h2>Added to your estate, never taken from it.</h2>
        <p class="lead">Two commitments about where the governed THINK layer sits: what it never takes away from you, and what it attaches to. Both are postures &mdash; how the platform is built, licensed, and designed to behave &mdash; not a connector inventory.</p>
      </div>

      <div id="deployment" class="home-posture">
        <h3>Deployment &mdash; your models, your systems, your control boundary</h3>
        <p class="home-posture__lead">Local, self-hosted and hosted routes have different acceptance boundaries. Host/model compatibility, migration and hosted availability need separate verification.</p>
        <ul class="think-consequence-list home-deployment-list">
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="spark" size={16} /></span><span class="think-consequence__step">Model-independent</span></span>
          <strong>Governance does not depend on one provider</strong>
          <p>Model profiles support selection for bounded tasks. Each host mapping and model change needs compatibility and quality checks.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="shield" size={16} /></span><span class="think-consequence__step">Local and sovereign</span></span>
          <strong>Define the data boundary explicitly</strong>
          <p>Local execution supports control, but models, tools, telemetry, remotes and backups can transmit data. Egress and retention policies remain necessary.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="layers" size={16} /></span><span class="think-consequence__step">Cloud where appropriate</span></span>
          <strong>Managed infrastructure without ceding control</strong>
          <p>Deploy services through managed infrastructure where it makes sense, while the control boundary stays explicit rather than assumed.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="check-badge" size={16} /></span><span class="think-consequence__step">Vendor-neutral standard</span></span>
          <strong>Governance you can inspect, not a black box</strong>
          <p>GSM is a candidate common definition model. Interchange requirements support inspectability; compatible consumers and draft-preserving migration still need acceptance.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="github" size={16} /></span><span class="think-consequence__step">Open and inspectable</span></span>
          <strong>Trust cannot be proprietary</strong>
          <p>GSM specification content exists in source, with publication pending. SAF local tools use Apache-2.0; SIE and the planned bridge have separate licenses. Inspect each product and its terms.</p>
        </li>
      </ul>
      </div>

      <div id="integration" class="home-posture">
        <h3>Integration &mdash; the definition layer, not a replacement</h3>
        <p class="home-posture__lead">Poesis does not ask you to rip anything out: governed THINK connects to the BUILD and RUN environments you already operate. The sourcing bench targets the artifacts your stack already produces, and the governed model it builds is a substrate your existing tools can consume.</p>
        <ul class="think-consequence-list home-integration-list">
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="repos" size={16} /></span><span class="think-consequence__step">Git repositories</span><DeliveryStatus state="planned" /></span>
          <strong>Planned candidates from pinned revisions</strong>
          <p>Supported analysis is planned to produce reviewable candidates. Acquisition, conflict reduction and governed registration are distinct steps.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="docs" size={16} /></span><span class="think-consequence__step">API contracts</span><DeliveryStatus state="planned" /></span>
          <strong>The interaction surface of your estate</strong>
          <p>Planned adapters describe declared interaction surfaces with explicit coverage. Contracts alone do not prove actual traffic or applicable obligations.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="list" size={16} /></span><span class="think-consequence__step">SBOMs</span><DeliveryStatus state="planned" /></span>
          <strong>Planned dependency evidence</strong>
          <p>SBOM analysis identifies declared dependencies in a supported scope. Presence does not prove deployed use, reachability or compliance.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="layers" size={16} /></span><span class="think-consequence__step">Infrastructure</span><DeliveryStatus state="planned" /></span>
          <strong>Separate intent from observations</strong>
          <p>Planned infrastructure sourcing reads declared configuration. Deployed identity, observation time and coverage are needed to describe what runs.</p>
        </li>
      </ul>
      <p class="home-integration-note"><DeliveryStatus state="planned" /> The sourcing bench is specified and in build. Named-tool connectors &mdash; ServiceNow CMDB, Backstage catalogs, EA repositories &mdash; follow as further knowledge sources feeding the same governed model.</p>
      </div>
    </div>
  </section>

  <section id="personas" class="section section--soft home-personas">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Actor types</p>
        <h2>Human and system actor types, shared definitions.</h2>
        <p class="lead">Architecture, delivery, operations, security and AI teams ask different questions about shared definitions. These are intended task perspectives, not already available integrated workflows.</p>
      </div>
      <ul class="think-consequence-list home-persona-list">
        {itProfiles.map((profile) => (
          <li id={profile.slug}>
            <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name={profile.icon} size={16} /></span><span class="think-consequence__step">{profile.name}</span></span>
            <strong>{profile.hook}</strong>
            <p>{profile.body}</p>
            <p><a href={`/usage?actor=${profile.slug}#use-cases`}>Related use cases &rarr;</a></p>
          </li>
        ))}
      </ul>
      <p><a href="/actors">All human and system actor types &rarr;</a></p>
    </div>
  </section>

  <section id="pilot" class="section section--ink home-pilot">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Start with one real workflow</p>
        <h2>Find where it hurts. Prove governed acceleration there.</h2>
        <p class="lead">Wherever your pain sits &mdash; stale knowledge, drifting delivery, opaque operations &mdash; it is the same THINK pathology surfacing in a different phase. Pick that workflow, make THINK explicit, then test how people, software, and AI perform across the governed loop.</p>
      </div>
      <ul class="think-consequence-list home-pilot-entries">
        <li id="use-case-think">
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="compass" size={16} /></span><span class="think-consequence__step">01 &middot; THINK</span></span>
          <strong>Know what your organization actually is</strong>
          <p>Explore directly authored definitions or planned reviewed candidates, with explicit ownership, revision identity and missing evidence.</p>
          <p class="home-usecase-proof"><span>Candidate pilot scope</span>Shared context and planned sourcing</p>
        </li>
        <li id="use-case-build">
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="layers" size={16} /></span><span class="think-consequence__step">02 &middot; BUILD</span></span>
          <strong>Build without rebuilding the intent</strong>
          <p>Test a named artifact family on a supported local workflow. Agree pins, substantive review and decision boundaries before evaluating repeatability.</p>
          <p class="home-usecase-proof"><span>Candidate pilot scope</span>Local controls and reviewed generation</p>
        </li>
        <li id="use-case-run">
          <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="play" size={16} /></span><span class="think-consequence__step">03 &middot; RUN</span></span>
          <strong>Define a bounded operational evaluation</strong>
          <p>Scope observations, Norms, unknown states and authority for a future loop. Measure coverage and delay; continuous detection is not an available general service.</p>
          <p class="home-usecase-proof"><span>Planned scope</span>Verdicts, observations and authorized response</p>
        </li>
      </ul>
      <ol class="think-consequence-list home-pilot-steps">
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon">01</span><span class="think-consequence__step">Frame THINK</span></span>
          <p>Identify purpose, actors, evidence, constraints, and human decisions.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon">02</span><span class="think-consequence__step">Govern THINK</span></span>
          <p>Source intent and RUN reality into a governed, machine-operable definition.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon">03</span><span class="think-consequence__step">Connect BUILD + RUN</span></span>
          <p>Let bounded software, mechanisms, people, and agents use the governed definition.</p>
        </li>
        <li>
          <span class="think-consequence__head"><span class="think-consequence__icon">04</span><span class="think-consequence__step">Evaluate the loop</span></span>
          <p>Measure BUILD and RUN speed, rework, control, and traceability against the starting point.</p>
        </li>
      </ol>
      <div class="hero-cta">
        <a class="btn btn-primary" href="/contact" data-demo-cta="homepage-pilot"><Icon name="play" /> Discuss a Pilot</a>
        <a class="btn btn-ghost" href="/services/consulting"><Icon name="briefcase" /> Explore transformation services</a>
      </div>
    </div>
  </section>

  <section id="portfolio" class="section home-platform">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">The Poesis platform</p>
        <h2>Explore the complete solutions and products map.</h2>
        <p class="lead">Specification, source content, local controls and planned integrations contribute in different ways. Explore each product for feature scope and historical version metadata.</p>
      </div>
      <ArchMap />
    </div>
  </section>

  <section id="services" class="section section--soft" aria-labelledby="services-heading">
    <div class="container">
      <div class="section-head">
        <h2 id="services-heading">Services</h2>
      </div>
      <ul class="think-consequence-list">
        {services.map((service) => (
          <li>
            <span class="think-consequence__head"><span class="think-consequence__icon"><Icon name="briefcase" size={16} /></span><span class="think-consequence__step">{service.category}</span></span>
            <h3><a href={service.href}>{service.label}</a></h3>
            <span class="claim-status">{service.availability}</span>
            <p>{service.summary}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section id="partnerships" class="section" aria-labelledby="partnerships-heading">
    <div class="container">
      <div class="section-head">
        <h2 id="partnerships-heading">Partnerships</h2>
        <p class="lead">Explore a joint experiment in governed context and model capabilities.</p>
      </div>
      <ul class="think-consequence-list">
        {partnerships.map((partnership) => (
          <li>
            <h3><a href={partnership.href}>{partnership.label}</a></h3>
            <p>Review the proposal, planned scope and evidence to establish together.</p>
          </li>
        ))}
      </ul>
    </div>
  </section>
</BaseLayout>
