---
import BaseLayout from '../../layouts/BaseLayout.astro';

const FLOW_META = {
  A: { hex: '#38bdf8', name: 'Context sourcing' },
  B: { hex: '#34d399', name: 'Governed inference grounding' },
  C: { hex: '#34d399', name: 'Governed inference deployment' },
  D: { hex: '#a78bfa', name: 'Model specialisation' },
  E: { hex: '#f59e0b', name: 'Model evaluation and assurance' },
  F: { hex: '#fb7185', name: 'Model qualification and routing' },
  G: { hex: '#94a3b8', name: 'Agentic delivery' },
};

function adherencePills(ids: string[]) {
  return ids
    .map((id) => {
      const meta = FLOW_META[id as keyof typeof FLOW_META];
      return `<button class="vf-pill" type="button" data-flow="${id}" style="--pill:${meta.hex}" aria-label="View adherence ${id} — ${meta.name}" title="${id} — ${meta.name}">${id}</button>`;
    })
    .join('');
}
---
<BaseLayout
  title="LLM Vendor Partnership — Value Proposal"
  description="What the Poesis stack brings to LLM capabilities — and what LLMs bring to Poesis. Models propose; humans dispose through ITIP; SIE evaluates constraints and executes governed Mechanisms."
  pageType="Partnership value proposal"
>
  <section class="hero">
    <div class="container">
      <p class="eyebrow">Partnerships</p>
      <h1>LLM Vendor Partnership — Value Proposal</h1>
      <p class="lead"><strong>Better context beats bigger models.</strong> Frontier models reason against statistical traces of the world — not against an explicit, causal, governed model of the world. That single gap drives both of today's ceilings: the <a href="#partnership-thesis"><strong>capability ceiling</strong> and the <strong>deployment ceiling</strong></a>. Poesis closes both through a systemic model of the world named <i>Generative System Model</i>, implemented by SIE, and surfaced through domain platforms such as ITIP — and model vendors are the natural partners on it.</p>
      <div class="reference-disclosure">
        <strong>How to read this proposal.</strong> Present tense describes the Poesis reference design and target operating model which might not yet be fully implemented or operational. Partnership adherences described below may therefore include work to advance Poesis components from their explicitly stated maturity levels.
      </div>
      <div class="hero-cta">
        <a class="btn btn-primary" href="/contact">Talk to us</a>
        <a class="btn btn-ghost" href="/solutions/gsm">The GSM standard</a>
        <a class="btn btn-ghost" href="/solutions/sie">The SIE engine</a>
      </div>
    </div>
  </section>

  <section class="section section--soft" id="partnership-thesis">
    <div class="container">
      <p class="eyebrow">The partnership thesis</p>
      <h2>Five reasons to partner.</h2>
      <p class="lead">One partnership addresses two limits: structured context raises the <strong>capability ceiling</strong>; enterprise platform, market access, platform advantage, and sovereignty raise the <strong>deployment ceiling</strong>.</p>
      <div class="thesis-grid">
        <div class="card card--feature" id="capability-ceiling">
          <span class="tag">Capability ceiling · Structured context</span>
          <h3>Better intelligence without bigger models.</h3>
          <p>A model that trains and reasons against explicit, causal, governed, and snapshot Definitions of the world works against explicit structure rather than relying only on correlation. That structure provides a substrate for impact analysis, counterfactual testing, and planning against modeled dependencies and much more...</p>
          <p class="thesis-mechanisms"><strong>Supported by adherences</strong> <span class="vf-pill-row" set:html={adherencePills(['B', 'D', 'E'])} /></p>
          <p class="thesis-tagline">Don't compete on larger models. Compete on better intelligence.</p>
        </div>
        <div class="card card--feature" id="deployment-ceiling">
          <span class="tag">Deployment ceiling · Enterprise platform</span>
          <h3>From language model to enterprise AI platform.</h3>
          <p>Companies buy solutions, not models. Partner models provide the intelligence; Poesis provides structured organizational context, human loop, deterministic evaluation of inference, grounded enterprise agentic development workflows, and audit evidence for accountability. Together they form a deployable enterprise agentic platform — able to act, not only answer.</p>
          <p class="thesis-mechanisms"><strong>Supported by adherences</strong> <span class="vf-pill-row" set:html={adherencePills(['B', 'C', 'F'])} /></p>
          <p class="thesis-tagline">Transform a model into an enterprise AI platform.</p>
        </div>
        <div class="card card--feature">
          <span class="tag">Deployment ceiling · Market access</span>
          <h3>A credible path into regulated industries.</h3>
          <p>Enterprises don't stall because AI isn't powerful enough — they stall because they can't trust it to act. Explainability, governance, auditability, and policy enforcement are entry conditions, and Poesis is designed to provide their shared context and control substrate.</p>
          <p class="thesis-mechanisms"><strong>Supported by adherences</strong> <span class="vf-pill-row" set:html={adherencePills(['C', 'E'])} /></p>
          <p class="thesis-tagline">Governance is the entry ticket to regulated industries.</p>
        </div>
        <div class="card card--feature">
          <span class="tag">Deployment ceiling · Platform advantage</span>
          <h3>A defensible advantage beyond model performance.</h3>
          <p>Grounded inference, governed execution, and model specialisation create a combined platform that is harder to commoditize than standalone inference. It moves the commercial relationship into AI governance, digital operating models, enterprise transformation, and regulated automation — increasing deal scope and strategic relevance.</p>
          <p class="thesis-mechanisms"><strong>Supported by adherences</strong> <span class="vf-pill-row" set:html={adherencePills(['B', 'C', 'D', 'E', 'F'])} /></p>
          <p class="thesis-tagline">Build a platform advantage, not simply a model advantage.</p>
        </div>
        <div class="card card--feature">
          <span class="tag">Deployment ceiling · Sovereignty</span>
          <h3>The foundation of a sovereign AI stack.</h3>
          <p>Not "a sovereign LLM" — a sovereign AI <em>stack</em>. You provide the sovereign intelligence. Poesis provides the governed context. Together they form the trusted infrastructure enterprises need to deploy AI at scale. <strong>As a neutral, model-independent substrate, GSM/SIE could be advanced as a candidate foundation for future European standardization of governed enterprise AI.</strong></p>
          <p class="thesis-mechanisms"><strong>Supported by adherences</strong> <span class="vf-pill-row" set:html={adherencePills(['A', 'B', 'C', 'D', 'E', 'F', 'G'])} /></p>
          <p class="thesis-tagline">Build the sovereign AI stack, not only a sovereign model.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <p class="eyebrow">The value mechanisms / adherences</p>
      <h2>How the partnership creates value.</h2>
      <p class="lead">An adherence is a concrete interface where partner capabilities and Poesis components combine to create reciprocal value. Each business outcome is supported by one or more such mechanisms.</p>
      <div class="value-grid">
        <button class="card value-card" data-flow="A" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">A · Structured context sourcing</span>
          <h3>Definitions sourced at scale</h3>
          <p>Partner models are designed to become KnowledgeSources over code repositories and, through additional adapters, other authoritative resources — broadening sourcing while preserving human disposition.</p>
          <span class="value-link">View flow details</span>
        </button>
        <button class="card value-card" data-flow="B" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">B · Augmented inference</span>
          <h3>Governed causal grounding</h3>
          <p>SIE can expose systematically sourced governed Definitions of the organization through a model-facing context and tool surface, enabling inference against explicit structure (causal, temporal, logical, semantic, evaluable) rather than prose documents alone.</p>
          <span class="value-link">View flow details</span>
        </button>
        <button class="card value-card" data-flow="C" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">C · Governed inference</span>
          <h3>Governed action in regulated estates</h3>
          <p>Models propose; SIE operates governance rules and evaluations deterministically; humans dispose throught domain application. The target result is accountable, traceable action.</p>
          <span class="value-link">View flow details</span>
        </button>
        <button class="card value-card" data-flow="D" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">D · Model specialisation</span>
          <h3>A governed specialisation substrate</h3>
          <p>Governed Definitions and Ascription history could provide typed domain material for controlled adaptation. Corpus construction, source provenance, rights enforcement, training integration, and measured uplift remain research work.</p>
          <span class="value-link">View flow details</span>
        </button>
        <button class="card value-card" data-flow="E" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">E · Model evaluation and assurance</span>
          <h3>Governed evidence of model capability</h3>
          <p>GSM contracts and SIE Mechanisms can turn defined enterprise tasks into reproducible evaluations, revealing model capability, failure boundaries, and regressions without requiring weight adaptation.</p>
          <span class="value-link">View flow details</span>
        </button>
        <button class="card value-card" data-flow="F" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">F · Model qualification and routing</span>
          <h3>The right model for each governed workload</h3>
          <p>A vendor-neutral catalog and routing contract can qualify models against workload needs, cost, and measured outcomes — creating evidence-based placement rather than one-model-fits-all selection.</p>
          <span class="value-link">View flow details</span>
        </button>
        <button class="card value-card" data-flow="G" type="button" aria-pressed="false" aria-controls="vf-detail">
          <span class="tag">G · Agentic delivery</span>
          <h3>A governed agentic delivery proof</h3>
          <p>Partner models power specialist agents under declared workflows; the deterministic harness enforces artifact, authorization, and workflow gates and record comparable delivery evidence.</p>
          <span class="value-link">View flow details</span>
        </button>
      </div>
    </div>
  </section>

  <section class="section section--soft" id="value-map">
    <div class="container">
      <p class="eyebrow">The value flows</p>
      <h2>An interactive map of the exchange.</h2>

      <div id="vf">
        <div class="vf-notices" role="region" aria-label="How to read the value map">
          <p class="vf-notice vf-notice-info"><strong>Explore the map.</strong> Click a flow to see its mutual value. Click an organisation, LLM, or Poesis component to see every adherence it holds. Expand a Poesis box for internal detail. Solid wires are partnership adherences; dotted orange wires are the organisation’s own governance loop.</p>
          <p class="vf-notice vf-notice-invariant"><strong>Core invariant:</strong> models <strong>PROPOSE</strong> (inferred, confidence &lt; 1.0) · accountable humans dispose through ITIP · SIE evaluates constraints and executes governed Mechanisms — <strong>PROPOSE / DISPOSE</strong>.</p>
        </div>

        <div class="vf-diagram" id="vf-diagram">
          <svg id="vf-wires" aria-hidden="true" focusable="false"></svg>

          <div class="vf-human">
            <div class="vf-gtitle">Organisation operations</div>
            <div class="vf-gsub">the customer organisation retains supervision, regulation, and governance over its own governed truth — that authority is never delegated to a model, and Poesis is the instrument through which it is exercised</div>
            <div class="vf-human-ops">
              <div class="vf-card vf-human-card" id="n-org-sup" data-component="Supervision" data-op="Supervision">
                <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                  <span class="vf-ctitle">Supervision</span>
                  <span class="vf-csub">disposes of AI-proposed Definitions / Ascriptions — reviews evidence, provenance, and evaluations, then approves or rejects before anything enters effect</span>
                </button>
              </div>
              <div class="vf-card vf-human-card" id="n-org-reg" data-component="Regulation" data-op="Regulation">
                <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                  <span class="vf-ctitle">Regulation</span>
                  <span class="vf-csub">sets and adjusts the constraints that bound the system — Directives, Norms, rights, consent, retention, and purpose limits on what models may receive and do</span>
                </button>
              </div>
              <div class="vf-card vf-human-card" id="n-org-gov" data-component="Governance" data-op="Governance">
                <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                  <span class="vf-ctitle">Governance</span>
                  <span class="vf-csub">owns intent and accountability — creation of Definitions / Ascriptions, evaluation and simulation of their compliance and impacts, and audit of AI and human action over them</span>
                </button>
              </div>
            </div>
            <p class="vf-human-note"><strong>No direct adherence exists between the organisation and the models.</strong> The organisation reaches them only through the governed perimeter — which is what makes supervision, regulation, and governance enforceable rather than declarative.</p>
          </div>

          <div class="vf-cols">

            <div class="vf-col">
              <div class="vf-group" id="g-partner">
                <div class="vf-gtitle">LLM operations</div>
                <div class="vf-gsub">vendor-neutral by design — any sovereign or frontier model: self-hosted, sovereign-hosted, or open-weight</div>

                <div class="vf-card" id="n-train" data-component="Model adaptation &amp; specialisation">
                  <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                    <span class="vf-ctitle">Model adaptation &amp; specialisation</span>
                    <span class="vf-csub">adapts through governed context, structured supervision, and evaluation signals — under explicit rights controls</span>
                  </button>
                </div>

                <div class="vf-card" id="n-infer" data-component="Grounded inference">
                  <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                    <span class="vf-ctitle">Grounded inference</span>
                    <span class="vf-csub">reasons against governed Definitions that model causal structure and carry traceable provenance — explicit context beyond document-only retrieval</span>
                  </button>
                </div>

                <div class="vf-card" id="n-eval" data-component="Model evaluation &amp; qualification">
                  <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                    <span class="vf-ctitle">Model evaluation &amp; qualification</span>
                    <span class="vf-csub">submits versioned model outputs and configurations for governed evaluation, comparison, and workload qualification</span>
                  </button>
                </div>
              </div>
            </div>

            <div class="vf-gutter"></div>

            <div class="vf-col">
              <div class="vf-group" id="g-poesis">
                <div class="vf-gtitle">Poesis stack</div>
                <div class="vf-gsub">one sovereign instance per organisation — tenant-owned, designed for future federation</div>

                <div class="vf-card" id="n-sie" data-component="SIE — Systemic Intelligence Engine">
                  <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                    <span class="vf-ctitle">SIE — Systemic Intelligence Engine</span>
                    <span class="vf-csub">reference implementation of GSM containing Definitions / Ascriptions of the organisation and implementing their governance lifecycle</span>
                  </button>
                  <button class="vf-fold-btn" type="button" data-fold="f-sie" aria-controls="f-sie" aria-expanded="false">▸ components</button>
                  <div class="vf-fold" id="f-sie" hidden>
                    <div class="vf-comp"><b>sie-definition-manager</b> — hosts GSM, Ascription lifecycle, governance grammar</div>
                    <div class="vf-comp"><b>sie-definition-blackboard-manager</b> — contribution registry and Blackboard-seal lifecycle</div>
                    <div class="vf-comp"><b>sie-operator</b> — executes in-effect Mechanisms and validates typed inputs and effects; automatic Norm preflight remains to be built</div>
                  </div>
                </div>

                <div class="vf-card vf-gate" id="n-itip" data-component="ITIP — domain platform (IT)">
                  <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                    <span class="vf-ctitle">ITIP — domain platform (IT)</span>
                    <span class="vf-csub">target domain surface for the <b>PROPOSE / DISPOSE gate to humans</b> — intent, approval, audit, and simulation</span>
                  </button>
                  <button class="vf-fold-btn" type="button" data-fold="f-itip" aria-controls="f-itip" aria-expanded="false">▸ components</button>
                  <div class="vf-fold" id="f-itip" hidden>
                    <div class="vf-comp"><b>itip-web-application</b> — planned human gate for intent, approval, simulation, appraisal, and audit</div>
                    <div class="vf-comp"><b>gsm-frameworks</b> — source GDPR, NIS2, DORA, AI Act, TOGAF, ISO, and other domain vocabulary into GSM Definitions</div>
                    <div class="vf-comp"><b>itip-definition-blackboard-*-sourcer</b> — designed to wrap LLMs / agents as KnowledgeSources over code repositories; other source classes require adapters</div>
                  </div>
                </div>

                <div class="vf-card" id="n-saf" data-component="SAFe Agentic Framework">
                  <button class="vf-card-select" type="button" aria-pressed="false" aria-controls="vf-detail">
                    <span class="vf-ctitle">SAFe Agentic Framework</span>
                    <span class="vf-csub">agentic organisation product delivery through declared workflows today, with a deterministic operational harness specified</span>
                  </button>
                  <button class="vf-fold-btn" type="button" data-fold="f-saf" aria-controls="f-saf" aria-expanded="false">▸ components</button>
                  <div class="vf-fold" id="f-saf" hidden>
                    <div class="vf-comp"><b>orchestrator-agent</b> — portfolio / program / team</div>
                    <div class="vf-comp"><b>specialist-agent</b> — architecture, dev, QA, security, docs agents</div>
                    <div class="vf-comp"><b>harness</b> — specified deterministic FSM, artifact, authorization, and gate checks; runtime implementation remains</div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="vf-mobile-flows" role="group" aria-label="Selectable value relationships">
            <button class="vf-mobile-flow" type="button" data-flow="A" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">A · Context sourcing</span>
              <strong>Sovereign LLM partner → Poesis stack</strong>
              <span>LLM-backed sourcing of GSM</span>
            </button>
            <button class="vf-mobile-flow" type="button" data-flow="B" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">B · Governed inference grounding</span>
              <strong>Poesis stack → Sovereign LLM partner</strong>
              <span>Governed causal context + tools</span>
            </button>
            <button class="vf-mobile-flow" type="button" data-flow="C" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">C · Governed inference deployment</span>
              <strong>Sovereign LLM partner → Poesis stack</strong>
              <span>Proposals → human disposition</span>
            </button>
            <button class="vf-mobile-flow" type="button" data-flow="D" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">D · Model specialisation</span>
              <strong>Poesis stack → Sovereign LLM partner</strong>
              <span>Governed material for model adaptation</span>
            </button>
            <button class="vf-mobile-flow" type="button" data-flow="E" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">E · Model evaluation and assurance</span>
              <strong>Poesis stack ↔ Sovereign LLM partner</strong>
              <span>Governed tests → comparable evidence</span>
            </button>
            <button class="vf-mobile-flow" type="button" data-flow="F" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">F · Model qualification and routing</span>
              <strong>Poesis stack → Sovereign LLM partner</strong>
              <span>Workload demand → qualified model placement</span>
            </button>
            <button class="vf-mobile-flow" type="button" data-flow="G" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">G · Agentic delivery</span>
              <strong>Sovereign LLM partner → Poesis stack</strong>
              <span>Agent bench builds the stack</span>
            </button>
            <button class="vf-mobile-flow vf-mobile-flow-loop" type="button" data-flow="reg" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">Governance loop · Regulation</span>
              <strong>Organisation → Poesis stack</strong>
              <span>Constraints set on the governed system</span>
            </button>
            <button class="vf-mobile-flow vf-mobile-flow-loop" type="button" data-flow="dis" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">Governance loop · Disposition</span>
              <strong>Organisation → Poesis stack</strong>
              <span>AI proposals approved or rejected</span>
            </button>
            <button class="vf-mobile-flow vf-mobile-flow-loop" type="button" data-flow="acc" aria-pressed="false" aria-controls="vf-detail">
              <span class="vf-mobile-flow-code">Governance loop · Intent and accountability</span>
              <strong>Organisation ↔ Poesis stack</strong>
              <span>Intent declared, evidence returned</span>
            </button>
          </div>
        </div>

        <div class="vf-detail" id="vf-detail" role="region" aria-live="polite" aria-atomic="false" tabindex="-1" hidden></div>
      </div>
    </div>
  </section>

  <section class="section" id="maturity">
    <div class="container">
      <div class="maturity-panel">
        <p class="eyebrow">Maturity disclosure</p>
        <h2>Operational maturity by value flow.</h2>
        <p>Current maturity and partnership work are stated separately for each flow. Implementation is part of the exchange: a partner can contribute model access, engineering, evaluation, and applied research to turn selected flows into measured operational proofs.</p>
        <div class="maturity-grid">
          <article>
            <span class="maturity-flow">Context sourcing</span>
            <strong class="maturity-status">Current maturity · Implemented substrate, designed client</strong>
            <p><strong>Partnership work:</strong> implement KnowledgeSources, model integration, client reduction, provenance transfer, and Definition Manager registration.</p>
          </article>
          <article>
            <span class="maturity-flow">Governed inference grounding</span>
            <strong class="maturity-status">Current maturity · Implemented semantic and execution foundations</strong>
            <p><strong>Partnership work:</strong> build the model-facing context and tool surface, then measure it against document-retrieval baselines.</p>
          </article>
          <article>
            <span class="maturity-flow">Governed inference deployment</span>
            <strong class="maturity-status">Current maturity · Implemented execution kernel, designed human gate</strong>
            <p><strong>Partnership work:</strong> assemble and validate the ITIP human gate, Norm and provenance evaluation, disposition audit, and regulated-estate proof.</p>
          </article>
          <article>
            <span class="maturity-flow">Model specialisation</span>
            <strong class="maturity-status">Current maturity · Research hypothesis</strong>
            <p><strong>Partnership work:</strong> agree corpus permissions and oracle criteria, run specialisation experiments, and measure uplift.</p>
          </article>
          <article>
            <span class="maturity-flow">Model evaluation and assurance</span>
            <strong class="maturity-status">Current maturity · Implemented primitives and integration proof</strong>
            <p><strong>Partnership work:</strong> define model evaluation contracts, datasets, baselines, trace capture, assurance criteria, and a repeatable service surface.</p>
          </article>
          <article>
            <span class="maturity-flow">Model qualification and routing</span>
            <strong class="maturity-status">Current maturity · Implemented catalog and contracts</strong>
            <p><strong>Partnership work:</strong> implement routing, host bindings, workload evidence collection, model-version governance, and outcome-based qualification.</p>
          </article>
          <article>
            <span class="maturity-flow">Agentic delivery</span>
            <strong class="maturity-status">Current maturity · Operational agent definitions, specified harness</strong>
            <p><strong>Partnership work:</strong> implement the harness runtime, bind partner models, collect controlled execution evidence, and establish repeatable outcome measures.</p>
          </article>
        </div>
        <div class="maturity-exchange">
          <span class="maturity-exchange-title">Cross-cutting operational adherence · Co-implementation</span>
          <p><strong>Direct value for Poesis:</strong> faster implementation, stronger experiments, benchmark evidence, and production feedback across the reference design.</p>
          <p><strong>Indirect value for the LLM vendor:</strong> a governed enterprise integration proving where its models perform, fail, and improve under accountable operating constraints.</p>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style is:global>
  .thesis-grid {
    display: grid; grid-template-columns: repeat(6, 1fr); gap: 1rem; margin-top: 1.5rem;
  }
  .thesis-grid > .card { grid-column: span 2; }
  .thesis-grid > .card:nth-child(4) { grid-column: 2 / span 2; }

  .thesis-tagline {
    margin-top: .9rem; padding-top: .7rem; border-top: 1px solid var(--line);
    color: var(--accent-ink); font-weight: 700; font-size: .95rem;
  }
  .thesis-mechanisms {
    margin-top: .9rem; padding-top: .7rem; border-top: 1px solid var(--line);
    color: var(--ink-soft); font-size: .8rem;
  }
  .thesis-mechanisms strong { color: var(--ink); }
  .vf-pill-row { display: inline-flex; flex-wrap: wrap; gap: .3rem; margin-left: .35rem; vertical-align: middle; }
  .vf-pill {
    display: inline-flex; align-items: center; justify-content: center;
    min-width: 1.5rem; height: 1.5rem; padding: 0 .1rem; border-radius: 999px;
    border: 1px solid var(--pill, var(--accent)); color: var(--pill, var(--accent));
    background: color-mix(in srgb, var(--pill, var(--accent)) 14%, transparent);
    font-family: var(--font-mono); font-size: .74rem; font-weight: 700; line-height: 1;
    cursor: pointer; transition: background .16s ease, transform .12s ease;
  }
  .vf-pill:hover { background: color-mix(in srgb, var(--pill, var(--accent)) 30%, transparent); transform: translateY(-1px); }
  .vf-pill:focus-visible { outline: 2px solid var(--pill, var(--accent)); outline-offset: 2px; }

  .reference-disclosure {
    max-width: 920px; margin-top: 1.2rem; padding: .8rem 1rem;
    border-left: 3px solid var(--accent); background: var(--accent-soft);
    color: var(--ink-soft); font-size: .9rem;
  }
  .reference-disclosure strong { color: var(--ink); }

  #vf .vf-mobile-flows { display: none; }

  .value-grid {
    display: grid; grid-template-columns: repeat(6, 1fr); gap: 1rem; margin-top: 1.5rem;
  }
  .value-card {
    grid-column: span 2; width: 100%; color: inherit; font: inherit; text-align: left; cursor: pointer;
    transition: border-color .16s ease, box-shadow .16s ease, transform .16s ease;
  }
  .value-card:nth-child(7) { grid-column: 3 / span 2; }
  .value-card:hover, .value-card:focus-visible {
    border-color: var(--accent); box-shadow: 0 0 0 2px rgba(45, 212, 191, .14); transform: translateY(-2px); outline: none;
  }
  .value-card h3 { margin-top: .65rem; }
  .value-card p { min-height: 6.6rem; }
  .value-link { display: block; color: var(--accent-ink); font-size: .82rem; font-weight: 700; }

  .maturity-panel {
    margin: 0; padding: 0;
  }
  .maturity-panel h2 { max-width: 760px; margin-top: .4rem; }
  .maturity-panel > p:not(.eyebrow) { max-width: 860px; color: var(--ink-soft); }
  .maturity-grid {
    display: grid; grid-template-columns: repeat(6, 1fr); gap: .8rem; margin-top: 1.2rem;
  }
  .maturity-grid article {
    grid-column: span 2; padding: .85rem .95rem; border-left: 3px solid var(--line-strong);
    background: rgba(255, 255, 255, .02);
  }
  .maturity-grid article:nth-child(7) { grid-column: 3 / span 2; }
  .maturity-grid p { margin: .45rem 0 0; color: var(--ink-soft); font-size: .84rem; }
  .maturity-grid p strong { color: var(--ink); }
  .maturity-flow {
    display: block; color: var(--accent-ink); font-family: var(--font-mono);
    font-size: .76rem; font-weight: 700;
  }
  .maturity-status { display: block; margin-top: .35rem; color: var(--ink); font-size: .88rem; }
  .maturity-exchange {
    display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-top: 1rem;
    padding: .9rem 1rem; border: 1px solid rgba(56, 189, 248, .4); border-radius: 8px;
    background: rgba(56, 189, 248, .08);
  }
  .maturity-exchange-title {
    grid-column: 1 / -1; color: var(--accent-ink); font-family: var(--font-mono);
    font-size: .76rem; font-weight: 700;
  }
  .maturity-exchange p { margin: 0; color: var(--ink-soft); font-size: .86rem; }
  .maturity-exchange strong { color: var(--ink); }

  #vf { margin-top: 1.5rem; }
  #vf .vf-notices { display: grid; grid-template-columns: 1fr 1fr; gap: .75rem; margin-bottom: 1rem; }
  #vf .vf-notice {
    margin: 0; border-radius: 10px; padding: .65rem .85rem; font-size: .86rem; color: var(--ink);
  }
  #vf .vf-notice-info { background: rgba(56, 189, 248, .08); border: 1px solid rgba(56, 189, 248, .4); }
  #vf .vf-notice-invariant { background: rgba(251, 191, 36, .10); border: 1px solid rgba(251, 191, 36, .45); }
  #vf .vf-human {
    border: 1px solid rgba(251, 146, 60, .5); background: rgba(251, 146, 60, .08); border-radius: 12px;
    padding: .75rem .9rem; margin: 0 0 2.4rem; position: relative; z-index: 1;
  }
  #vf .vf-human .vf-gtitle { color: #fb923c; }
  #vf .vf-human-ops { display: grid; grid-template-columns: repeat(3, 1fr); gap: .6rem; }
  #vf .vf-human-card { margin-top: 0; background: rgba(251, 146, 60, .06); border-color: rgba(251, 146, 60, .35); }
  #vf .vf-human-card:hover, #vf .vf-human-card:has(.vf-card-select:focus-visible) { border-color: rgba(251, 146, 60, .7); box-shadow: none; }
  #vf .vf-human-card.vf-selected { border-color: #fb923c; box-shadow: 0 0 0 2px rgba(251, 146, 60, .2); }
  #vf .vf-human-card .vf-ctitle { color: #fb923c; }
  #vf .vf-human-card .vf-card-select:focus-visible { outline-color: #fb923c; }
  #vf .vf-human-note { margin: .65rem 0 0; font-size: .78rem; color: var(--ink-soft); }
  #vf .vf-human-note strong { color: #fb923c; }

  #vf .vf-diagram { position: relative; }
  #vf #vf-wires { position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; z-index: 2; }
  #vf .vf-cols { position: relative; display: grid; grid-template-columns: 1fr 250px 1fr; z-index: 1; }
  #vf .vf-col { display: flex; flex-direction: column; gap: 1rem; }
  #vf .vf-gutter { pointer-events: none; }

  #vf .vf-group {
    border: 1px solid var(--line-strong); border-radius: 14px; background: var(--bg-soft);
    padding: .85rem .95rem;
  }
  #vf .vf-gtitle { font-weight: 700; color: #fff; font-size: .98rem; }
  #vf .vf-gsub { color: var(--muted); font-size: .8rem; margin-bottom: .55rem; }

  #vf .vf-card {
    border: 1px solid var(--line-strong); border-radius: 10px; background: var(--bg-ink);
    padding: .6rem .75rem; margin-top: .6rem; transition: border-color .16s ease, box-shadow .16s ease;
  }
  #vf .vf-card:hover, #vf .vf-card:has(.vf-card-select:focus-visible) { border-color: var(--accent); box-shadow: 0 0 0 2px rgba(45, 212, 191, .12); }
  #vf .vf-card.vf-selected { border-color: var(--accent); box-shadow: 0 0 0 2px rgba(45, 212, 191, .18); }
  #vf .vf-card-select {
    display: block; width: 100%; padding: 0; border: 0; background: transparent;
    color: inherit; font: inherit; text-align: left; cursor: pointer;
  }
  #vf .vf-card-select:focus-visible { outline: 2px solid var(--accent); outline-offset: 4px; border-radius: 3px; }
  #vf .vf-ctitle { display: block; font-weight: 600; color: var(--ink); font-size: .89rem; }
  #vf .vf-csub { display: block; color: var(--muted); font-size: .78rem; margin-top: .18rem; }
  #vf .vf-gate { background: rgba(251, 191, 36, .07); border-color: rgba(251, 191, 36, .45); }

  #vf .vf-fold-btn {
    margin-top: .5rem; border: 1px solid var(--line-strong); background: transparent; color: var(--accent-ink);
    border-radius: 6px; font-size: .74rem; padding: .18rem .55rem; cursor: pointer;
  }
  #vf .vf-fold-btn:hover { background: var(--accent-soft); }
  #vf .vf-fold { display: none; margin-top: .55rem; border-top: 1px dashed var(--line-strong); padding-top: .55rem; }
  #vf .vf-fold.open { display: block; }
  #vf .vf-comp {
    font-size: .76rem; color: var(--ink-soft); padding: .3rem .5rem; border-left: 3px solid var(--line-strong);
    margin-top: .35rem; background: var(--bg-soft); border-radius: 0 6px 6px 0;
  }
  #vf .vf-comp b { font-family: var(--font-mono); font-size: .72rem; color: var(--ink); }

  #vf .vf-flow-hit { pointer-events: stroke; cursor: pointer; }
  #vf .vf-flow-loop { stroke-linejoin: round; stroke-linecap: round; }

  #vf .vf-detail {
    margin-top: 1.4rem; border: 1px solid var(--line-strong); border-radius: 14px;
    background: var(--bg-soft); padding: 1rem 1.1rem;
  }
  #vf .vf-detail h3 { font-size: 1.02rem; }
  #vf .vf-maturity {
    margin: .45rem 0 .65rem; padding: .5rem .65rem; border-left: 3px solid var(--accent);
    background: var(--accent-soft); color: var(--ink-soft); font-size: .8rem;
  }
  #vf .vf-maturity strong { color: var(--ink); }
  #vf .vf-dsub { color: var(--ink-soft); font-size: .87rem; margin-bottom: .85rem; }
  #vf .vf-adherence + .vf-adherence { border-top: 1px solid var(--line-strong); margin-top: 1rem; padding-top: 1rem; }
  #vf .vf-adherence h4 { font-size: .92rem; }
  #vf .vf-schema {
    margin: 0 0 1rem; padding: .7rem .75rem; border: 1px solid var(--line-strong);
    border-radius: 10px; background: var(--bg-ink);
  }
  #vf .vf-schema-body { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: .8rem 1.4rem; align-items: start; }
  #vf .vf-schema-graph { position: relative; padding: .15rem 2.6rem; min-width: 0; }
  #vf .vf-schema-wires { position: absolute; inset: 0; pointer-events: none; }
  #vf .vf-schema-nodes { position: relative; display: flex; flex-direction: column; align-items: flex-start; gap: 1.7rem; }
  #vf .vf-node {
    display: inline-flex; flex-direction: column; align-items: flex-start; gap: .1rem;
    padding: .34rem .5rem; border-radius: 7px; border: 1px solid transparent; max-width: 200px;
    font-family: var(--font-mono); font-size: .7rem; line-height: 1.25; overflow-wrap: anywhere;
  }
  #vf .vf-node-poesis { color: var(--accent-ink); border-color: rgba(45, 212, 191, .4); background: rgba(45, 212, 191, .1); }
  #vf .vf-node-llm { color: #7dd3fc; border-color: rgba(56, 189, 248, .4); background: rgba(56, 189, 248, .1); }
  #vf .vf-node-human { color: #fdba74; border-color: rgba(251, 146, 60, .5); background: rgba(251, 146, 60, .14); }
  #vf .vf-node-material {
    color: var(--muted); border-color: var(--line-strong); border-style: dashed;
    background: var(--bg-soft); font-family: inherit; font-size: .73rem;
  }
  #vf .vf-node-todo {
    font-family: inherit; font-size: .58rem; letter-spacing: .08em; text-transform: uppercase; opacity: .75;
  }
  #vf .vf-schema-steps { margin: 0; padding: 0; list-style: none; display: grid; gap: .42rem; align-content: start; }
  #vf .vf-schema-steps li { display: flex; gap: .5rem; align-items: baseline; font-size: .72rem; line-height: 1.35; color: var(--muted); }
  #vf .vf-step-num {
    flex: none; width: 1.05rem; height: 1.05rem; border-radius: 50%; align-self: flex-start;
    display: inline-flex; align-items: center; justify-content: center;
    border: 1px solid var(--line-strong); background: var(--bg-soft); color: var(--ink);
    font-family: var(--font-mono); font-size: .58rem;
  }
  #vf .vf-schema-legend { display: flex; flex-wrap: wrap; align-items: center; gap: .35rem; margin: 0 0 .6rem; }
  #vf .vf-schema-legend .vf-node { font-family: inherit; font-size: .63rem; padding: .12rem .38rem; }
  #vf .vf-schema-feedback {
    margin: .55rem 0 0; padding-top: .5rem; border-top: 1px dashed var(--line-strong);
    font-size: .73rem; color: var(--muted);
  }
  #vf .vf-dcols { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
  #vf .vf-dcols h5, #vf .vf-shared h5 { font-size: .78rem; margin: 0 0 .35rem; color: var(--muted); text-transform: uppercase; letter-spacing: .05em; }
  #vf .vf-dcols ul, #vf .vf-shared ul { margin: 0; padding-left: 1.1rem; font-size: .86rem; color: var(--ink-soft); }
  #vf .vf-dcols li, #vf .vf-shared li { margin-bottom: .3rem; }
  #vf .vf-shared {
    margin-top: 1rem; padding: .7rem .85rem; border: 1px dashed var(--line-strong);
    border-radius: 10px; background: var(--bg-ink);
  }
  #vf .vf-shared li:last-child { margin-bottom: 0; }
  #vf .vf-comps { margin-top: .9rem; font-size: .78rem; color: var(--muted); }
  #vf .vf-comps b { font-family: var(--font-mono); font-size: .74rem; color: var(--ink); }

  @media (max-width: 860px) {
    .thesis-grid { grid-template-columns: 1fr; }
    .thesis-grid > .card, .thesis-grid > .card:nth-child(4) { grid-column: auto; }
    .value-grid { grid-template-columns: 1fr; }
    .value-card, .value-card:nth-child(7) { grid-column: auto; }
    .value-card p { min-height: 0; }
    .maturity-grid { grid-template-columns: 1fr; }
    .maturity-grid article, .maturity-grid article:nth-child(7) { grid-column: auto; }
    .maturity-exchange { grid-template-columns: 1fr; }
    .maturity-exchange-title { grid-column: auto; }
    #vf .vf-notices { grid-template-columns: 1fr; }
    #vf .vf-human-ops { grid-template-columns: 1fr; }
    #vf .vf-cols { grid-template-columns: 1fr; }
    #vf .vf-gutter { display: none; }
    #vf #vf-wires { display: none; }
    #vf .vf-dcols { grid-template-columns: 1fr; }
    #vf .vf-schema-body { grid-template-columns: 1fr; }
    #vf .vf-mobile-flows { display: grid; gap: .65rem; margin-top: 1rem; }
    #vf .vf-mobile-flow {
      display: grid; gap: .18rem; width: 100%; min-width: 0; padding: .8rem .9rem;
      border: 1px solid var(--line-strong); border-left: 3px solid var(--accent); border-radius: 8px;
      background: var(--bg-soft); color: var(--ink-soft); font: inherit; font-size: .82rem;
      text-align: left; cursor: pointer; overflow-wrap: anywhere;
    }
    #vf .vf-mobile-flow-loop { border-left-color: #fb923c; }
    #vf .vf-mobile-flow:hover, #vf .vf-mobile-flow[aria-pressed="true"] {
      border-color: var(--accent); background: var(--accent-soft);
    }
    #vf .vf-mobile-flow strong { color: var(--ink); font-size: .87rem; }
    #vf .vf-mobile-flow-code { color: var(--accent-ink); font-family: var(--font-mono); font-size: .74rem; font-weight: 700; }
  }
</style>

<script>
  const FLOWS = [
    {
      id: 'A', hex: '#38bdf8', from: 'n-infer', to: 'n-sie', lane: 32,
      label: 'LLM-backed sourcing of GSM',
      title: 'A — Context sourcing: LLMs feed GSM (LLM → Poesis)',
      maturity: '<strong>Current maturity:</strong> the generic Blackboard REST substrate, schema validation, seal lifecycle, immutability, and deterministic stream ordering are implemented. <strong>Partnership work:</strong> implement the repository KnowledgeSources, LLM client, reduction, provenance transfer, and registration path.',
      sub: 'The repository sourcer is designed to wrap partner models as KnowledgeSources over code repositories. Additional source classes require explicit adapters. ITIP contribution schemas require confidence below 1.0, evidence, and provenance; the generic Blackboard validates whichever client-declared slot schema applies, freezes accepted contributions on seal, and returns them in deterministic order. A client reducer then prepares candidate Ascription proposals for human disposition.',
      schema: [
        { n: 'Repository artifacts', side: 'material' },
        { n: 'itip-definition-blackboard-*-sourcer', todo: true, via: 'indexes code, configuration, documentation' },
        { n: 'Grounded inference', side: 'llm', via: 'extraction task + tool contracts' },
        { n: 'sie-definition-blackboard-manager', via: 'typed contributions — evidence, provenance, confidence &lt; 1.0' },
        { n: 'itip-definition-blackboard-*-sourcer', todo: true, via: 'sealed, deterministically ordered stream' },
        { n: 'Supervision', side: 'human', via: 'reduced candidate Ascription proposals' },
        { n: 'sie-definition-manager', via: 'approved Definitions placed in effect' },
      ],
      feedback: 'Extraction accuracy, evidence use, tool calling, and confidence calibration are measured and returned to the partner.',
      partner: ['Applied-AI evidence for extracting typed Definitions and relationships from repository artifacts', 'Measured feedback on extraction accuracy, evidence use, tool calling, and confidence calibration'],
      poesis: ['Broader sourcing coverage without granting models authority over governed truth', 'A model-agnostic sourcing substrate that widens with each connected KnowledgeSource rather than with any single model generation'],
      shared: ['Repository knowledge surfaced as typed, attributable Definitions instead of remaining tacit', 'Every accepted contribution schema-validated, provenance-bearing, and replayable for audit'],
      comps: 'Implemented in <b>sie-definition-blackboard-manager</b>; designed through <b>itip-definition-blackboard-*-sourcer</b> → client reduction and human disposition → <b>sie-definition-manager</b>.',
    },
    {
      id: 'B', hex: '#34d399', from: 'n-sie', to: 'n-infer', lane: -32,
      label: 'governed causal context + tools',
      title: 'B — Governed inference grounding (Poesis → LLM)',
      maturity: '<strong>Current maturity:</strong> implemented foundations. <strong>Partnership work:</strong> build the model-facing context and tool surface, then measure it against document-retrieval baselines.',
      sub: 'Definition Manager and Operator provide the semantic and execution foundations for serving selected in-effect Definitions, Norms, and explicitly modeled dependencies. A model-facing context and tool facade is not yet implemented. Once built, it can let models inspect types, trace Interactions, retrieve applicable constraints, and submit outputs for explicit evaluation. This changes available evidence and operations; it does not change model weights or prove causal reasoning.',
      schema: [
        { n: 'gsm-frameworks' },
        { n: 'sie-definition-manager', via: 'sourced obligation and domain vocabulary' },
        { n: 'sie-operator', via: 'in-effect Definitions, Norms, modeled Interactions' },
        { n: 'Grounded inference', side: 'llm', via: 'context selection, provenance projection, tool contracts — facade not implemented' },
        { n: 'Grounded answer', side: 'material', via: 'inspects types, traces Interactions, retrieves constraints' },
      ],
      feedback: 'Evaluated against document-retrieval baselines: unsupported claims and dependency-trace accuracy.',
      partner: ['Runtime specialisation through governed context and tools without retraining the model', 'Evaluation target: fewer unsupported claims and more accurate dependency traces than document-retrieval baselines'],
      poesis: ['A model-neutral context and tool contract that can be tested across conforming models', 'Grounding value that accrues to the governed Definition base rather than to any single model generation'],
      shared: ['Answers grounded in the customer organisation’s current, governed Definitions rather than an undifferentiated document corpus', 'Grounding scope bounded by the customer organisation’s own definition base, keeping context inside its governance perimeter'],
      comps: 'Foundations implemented in <b>sie-definition-manager</b> and <b>sie-operator</b>; model-facing context selection, provenance projection, and tool contracts remain to be built.',
    },
    {
      id: 'C', hex: '#34d399', from: 'n-infer', to: 'n-itip', lane: 0,
      label: 'proposals → human disposition',
      title: 'C — Governed inference deployment (LLM → Poesis)',
      maturity: '<strong>Current maturity:</strong> partial foundation. <strong>Partnership work:</strong> assemble and validate the ITIP human gate, Norm and provenance evaluation, disposition audit, and regulated-estate proof.',
      sub: 'Every model output entering a governed action path remains a proposal. The target ITIP gate presents evidence, provenance, and evaluations to an accountable human for disposition; that gate is not yet implemented. Operator already executes only in-effect executable Mechanisms (ACTIVE or DEPRECATED) and performs deterministic schema and rule checks. It does not yet perform automatic Norm preflight. These checks establish conformance to explicit contracts, not the truth of an inference.',
      schema: [
        { n: 'Grounded inference', side: 'llm' },
        { n: 'sie-operator', via: 'a proposal, never an action' },
        { n: 'itip-web-application', todo: true, via: 'schema and rule conformance checked' },
        { n: 'Supervision', side: 'human', via: 'evidence, provenance, evaluations presented' },
        { n: 'sie-operator', via: 'approved — dispatch to in-effect Mechanisms' },
        { n: 'sie-definition-manager', via: 'execution effects and lifecycle transition' },
        { n: 'itip-web-application', todo: true, via: 'decision, evidence, and execution composed into audit' },
      ],
      partner: ['A deployable control pattern for supervised model action in regulated environments', 'An assurance case grounded in traceable proposals, deterministic checks, human decisions, and execution evidence'],
      poesis: ['A governed disposition gate as the single controlled entry point for model-proposed action', 'Disposition and outcome records can support later evaluation, but become learning labels only under defined tasks, measures, and rights controls'],
      shared: ['AI-assisted action constrained by the customer organisation’s governed Definitions and explicit human authority', 'An accountable human decision, with the evidence it rested on, recorded for every model-proposed change'],
      comps: 'Execution kernel implemented in <b>sie-operator</b> with lifecycle APIs in <b>sie-definition-manager</b>; the <b>itip-web-application</b> human gate, composed audit, and automatic Norm preflight remain to be built.',
    },
    {
      id: 'D', hex: '#a78bfa', from: 'n-sie', to: 'n-train', lane: 0,
      label: 'governed material for model adaptation',
      title: 'D — Model adaptation & specialisation substrate (Poesis → LLM)',
      maturity: '<strong>Current maturity:</strong> research hypothesis. <strong>Partnership work:</strong> define adaptation tasks, corpus permissions, evaluation criteria, and baselines; then run controlled experiments and measure uplift.',
      sub: 'Governed Definitions and Ascription history could supply typed domain examples for continued pre-training, supervised fine-tuning, preference tuning, or distillation, but no corpus export or training integration exists. Mechanisms and Interactions encode modeled causal relations; lifecycle timestamps record temporal history, not causal proof. Human approvals and rejections are governance decisions, not automatically model-quality labels.',
      schema: [
        { n: 'sie-definition-manager' },
        { n: 'Regulation', side: 'human', via: 'candidate corpus — typed identities, relations, Ascription history' },
        { n: 'Model adaptation &amp; specialisation', side: 'llm', via: 'rights-bounded corpus export — not implemented' },
        { n: 'Model evaluation &amp; qualification', side: 'llm', via: 'adapted weights' },
        { n: 'sie-operator', via: 'controlled evaluation against runtime grounding alone' },
      ],
      feedback: 'The result decides whether governed material should leave the runtime boundary at all, and the decision stays revocable.',
      partner: ['A potential adaptation corpus containing typed identities, modeled relationships, and lifecycle state', 'Controlled experiments comparing weight adaptation with runtime grounding alone'],
      poesis: ['Controlled evidence of whether weight adaptation improves sourcing, grounded inference, or other defined tasks over runtime grounding alone', 'A defensible position on when governed material should — and should not — leave the runtime boundary'],
      shared: ['Tenant ownership, licensing, consent, privacy, retention, and purpose limitation govern every corpus and feedback use', 'No governed material contributes to model adaptation without an explicit, revocable decision by the customer organisation'],
      comps: 'Potential source material exists in <b>sie-definition-manager</b>; corpus selection, provenance integration, serialization, rights enforcement, training integration, and outcome measurement remain research work.',
    },
    {
      id: 'G', hex: '#94a3b8', from: 'n-infer', to: 'n-saf', lane: 0, dashed: true,
      label: 'agent bench builds the stack',
      title: 'G — Agentic delivery: LLM agents build Poesis itself (LLM → Poesis)',
      maturity: '<strong>Current maturity:</strong> evolving operational proof. <strong>Partnership work:</strong> adopt partner models, collect controlled execution evidence, and establish repeatable outcome measures.',
      sub: 'Partner models already power the framework’s orchestrators and specialist agents through declarative agent and workflow definitions. The deterministic harness is specified but its runtime is not implemented. Once operational, its artifact, authorization, precondition, and postcondition gates can produce comparable execution evidence without treating agent output as self-authorizing.',
      schema: [
        { n: 'Governance', side: 'human' },
        { n: 'orchestrator-agent', via: 'declarative agent and workflow definitions' },
        { n: 'Grounded inference', side: 'llm', via: 'task context and tool calls' },
        { n: 'specialist-agent', via: 'architecture, dev, QA, security, docs work' },
        { n: 'harness', todo: true, via: 'artifact, authorization, precondition, postcondition gates' },
        { n: 'Supervision', side: 'human', via: 'gated candidate changes' },
        { n: 'Poesis stack artifacts', side: 'material', via: 'released code, schemas, design-time models, documentation' },
      ],
      feedback: 'Task success, rework, gate failures, cost, latency, and human intervention are measured per run.',
      partner: ['A living reference for supervised multi-agent engineering using partner models', 'A planned evidence surface for task success, rework, gate failures, cost, latency, and human intervention'],
      poesis: ['Faster delivery where measured outcomes demonstrate an advantage over the baseline workflow', 'Auditable delivery evidence governed by deterministic gates and human approval'],
      comps: 'Agent definitions, workflows, model catalog, and harness contracts exist in the <b>SAFe Agentic Framework</b>; the deterministic harness runtime and persisted evidence collection remain to be implemented.',
    },
    {
      id: 'E', hex: '#f59e0b', from: 'n-sie', to: 'n-eval', lane: 28,
      label: 'governed tests → comparable model evidence',
      title: 'E — Model evaluation and assurance (Poesis ↔ LLM)',
      maturity: '<strong>Current maturity:</strong> schema validation, typed Mechanism execution, lifecycle audit, and an ITIP appraisal integration proof are implemented. <strong>Partnership work:</strong> define model evaluation envelopes, datasets, baselines, assurance criteria, and a repeatable service surface.',
      sub: 'A vendor submits versioned model outputs and inference configurations against defined enterprise tasks. GSM schemas and SIE Mechanisms can evaluate only properties made explicit by those contracts, producing reproducible findings and traces. This can reveal capability, failure boundaries, and regressions without changing model weights; it does not turn every governance rule into ground truth or establish inference truth beyond the evaluated contract.',
      schema: [
        { n: 'sie-definition-manager' },
        { n: 'Model evaluation &amp; qualification', side: 'llm', via: 'enterprise task envelopes — GSM schemas, Mechanisms, expected outcomes' },
        { n: 'sie-operator', via: 'versioned model outputs and inference configuration' },
        { n: 'itip-web-application', todo: true, via: 'deterministic evaluation of contracted properties' },
        { n: 'Governance', side: 'human', via: 'reproducible findings and traces — capability, failure boundaries, regressions' },
      ],
      feedback: 'Findings return to the vendor as workload-specific evidence rather than general benchmark claims.',
      partner: ['Workload-specific evidence about model strengths, regressions, and failure boundaries', 'A governed assurance case grounded in reproducible tests rather than general benchmark claims'],
      poesis: ['Reusable evaluation assets derived from explicit enterprise schemas, Mechanisms, and expected outcomes', 'An evaluation surface that stays meaningful as models change, because it is anchored in enterprise contracts rather than vendor benchmarks'],
      shared: ['Comparable evidence for selecting models and validating upgrades before governed deployment', 'Regressions detected against the customer organisation’s own workloads rather than against general benchmarks'],
      comps: 'Builds on <b>sie-operator</b> typed execution and appraisal Mechanisms + <b>sie-definition-manager</b> schemas and lifecycle history; generic model-evaluation APIs and trace contracts remain to be designed.',
    },
    {
      id: 'F', hex: '#fb7185', from: 'n-saf', to: 'n-infer', lane: -28,
      label: 'workload demand → qualified model placement',
      title: 'F — Model qualification and workload routing (Poesis → LLM)',
      maturity: '<strong>Current maturity:</strong> a vendor-neutral model catalog, capability taxonomy, workload declarations, and routing output contracts exist. <strong>Partnership work:</strong> implement the resolver runtime, host bindings, model-version governance, and outcome evidence collection.',
      sub: 'Governed workloads declare capability demand; a deterministic resolver is specified to score canonical model profiles and break ties by cost. Runtime qualification should then incorporate measured task success, schema adherence, rework, latency, cost, and human intervention. The current catalog is configuration, not proof of model fitness, and the harness that performs routing is not yet implemented.',
      schema: [
        { n: 'orchestrator-agent' },
        { n: 'harness', todo: true, via: 'workload capability demand declared' },
        { n: 'Grounded inference', side: 'llm', via: 'resolver scores model profiles, binds host — dispatch not implemented' },
        { n: 'harness', todo: true, via: 'task success, schema adherence, rework, latency, cost, intervention' },
        { n: 'Regulation', side: 'human', via: 'qualification record — placement becomes an evidence decision' },
      ],
      feedback: 'Qualification feeds the next resolution, so placement changes with measured behaviour rather than with a procurement default.',
      partner: ['Evidence-based placement on workloads where the partner model demonstrates comparative value', 'Structured feedback about capability gaps, reliability, cost, and intervention demand'],
      poesis: ['Model-neutral routing aligned to workload needs instead of a single-provider default', 'A qualification record that makes model placement an evidence decision rather than a procurement default'],
      shared: ['Controlled model upgrades and substitutions based on declared demand and measured outcomes', 'No structural dependence on a single provider for the customer organisation’s governed workloads'],
      comps: 'Catalog and contracts exist in the <b>SAFe Agentic Framework</b>; resolver implementation, host adapter bindings, dispatch telemetry, and qualification policy remain to be built.',
    },
    {
      id: 'reg', loop: true, rail: -9, hex: '#fb923c', from: 'n-org-reg', to: 'n-sie', lane: -66,
      label: 'constraints set on the governed system',
      title: 'Governance loop · Regulation — the organisation bounds what the system may do (Organisation → Poesis)',
      maturity: '<strong>Current maturity:</strong> the governance grammar, Directive and Norm schemas, and the Ascription lifecycle that places constraints in effect are implemented; framework sourcing produces typed vocabulary. <strong>Remaining work:</strong> automatic Norm preflight in the Operator, and appraisal-driven revision of constraints.',
      sub: 'Directives and Norms are how the organisation states, explicitly and in a single grammar, what it and its models may do. Framework sourcing turns external obligation — GDPR, NIS2, DORA, AI Act, TOGAF, ISO — into the same typed vocabulary as internal policy, so regulatory constraint and internal intent are not two disconnected registers. Nothing in this flow reaches a model directly: constraints bound the LLM adherences from inside the perimeter.',
      schema: [
        { n: 'gsm-frameworks' },
        { n: 'Regulation', side: 'human', via: 'GDPR, NIS2, DORA, AI Act, TOGAF, ISO vocabulary' },
        { n: 'sie-definition-manager', via: 'Directives and Norms authored' },
        { n: 'Governance', side: 'human', via: 'lifecycle transition proposed' },
        { n: 'sie-operator', via: 'constraints placed in effect' },
        { n: 'Grounded inference', side: 'llm', via: 'typed checks — automatic Norm preflight planned' },
      ],
      feedback: 'Appraisal of outcomes against Norms is what tells the organisation when its own constraints need to change.',
      organisation: ['Regulatory obligation and internal policy expressed in one enforceable grammar rather than in documents no system reads', 'Constraints that bound model behaviour without depending on any model’s cooperation'],
      poesis: ['A single constraint grammar that every adherence in this map is evaluated against', 'Governed vocabulary that widens through framework sourcing without changing the runtime'],
      comps: 'Implemented in <b>sie-definition-manager</b> with vocabulary sourced through <b>gsm-frameworks</b>; enforcement in <b>sie-operator</b>, where automatic Norm preflight remains to be built.',
    },
    {
      id: 'dis', loop: true, rail: 0, hex: '#fb923c', from: 'n-org-sup', to: 'n-itip', lane: -6,
      label: 'AI proposals approved or rejected',
      title: 'Governance loop · Disposition — nothing AI-proposed enters effect without an accountable decision (Organisation → Poesis)',
      maturity: '<strong>Current maturity:</strong> the Ascription lifecycle and its transitions are implemented, and the Blackboard produces sealed, replayable candidate contributions. <strong>Remaining work:</strong> the ITIP disposition surface that presents evidence, provenance, and evaluations to an accountable human.',
      sub: 'This is the DISPOSE half of PROPOSE / DISPOSE, and it is the single controlled entry point through which model work becomes governed truth. A proposal carries confidence below 1.0 together with its evidence and provenance; a named human decides. The decision is recorded with the evidence it rested on, which is what makes supervision auditable rather than merely asserted.',
      schema: [
        { n: 'Grounded inference', side: 'llm' },
        { n: 'sie-definition-blackboard-manager', via: 'proposals — confidence &lt; 1.0, evidence, provenance' },
        { n: 'itip-web-application', todo: true, via: 'sealed, replayable contributions' },
        { n: 'Supervision', side: 'human', via: 'evidence, provenance, evaluations presented' },
        { n: 'sie-definition-manager', via: 'approve or reject' },
        { n: 'itip-web-application', todo: true, via: 'Ascription transition recorded with its evidence' },
      ],
      feedback: 'Rejection rates and rejection reasons are the organisation’s sharpest signal about model fitness.',
      organisation: ['A single controlled entry point for model-proposed change, rather than model output leaking into systems through many uncontrolled paths', 'Supervision that produces evidence — every decision recorded with what it rested on'],
      poesis: ['A disposition gate that keeps model authority bounded without slowing the sourcing surface', 'Disposition records that can later support evaluation, under defined tasks, measures, and rights controls'],
      comps: 'Lifecycle transitions implemented in <b>sie-definition-manager</b>, sealed candidates in <b>sie-definition-blackboard-manager</b>; the <b>itip-web-application</b> disposition surface remains to be built.',
    },
    {
      id: 'acc', loop: true, both: true, rail: 9, hex: '#fb923c', from: 'n-org-gov', to: 'n-itip', lane: 54,
      label: 'intent declared, evidence returned',
      title: 'Governance loop · Intent and accountability — the organisation defines itself and answers for the result (Organisation ↔ Poesis)',
      maturity: '<strong>Current maturity:</strong> Definition and Ascription authoring, lifecycle history, and typed Mechanism execution are implemented, with an ITIP appraisal integration proof. <strong>Remaining work:</strong> the appraisal, simulation, and audit surfaces that return this evidence to the organisation.',
      sub: 'This is the flow that closes the map. Intent becomes typed Definitions and Ascriptions; those govern operation; operation produces appraisal, simulation, and audit evidence; that evidence returns to the intent that produced it. Without this return path, regulation and disposition would be open-loop — constraint and action with no corrective signal. Systemic viability depends on the return, not on the outbound half.',
      schema: [
        { n: 'Governance', side: 'human' },
        { n: 'itip-web-application', todo: true, via: 'intent — what the organisation means to be and to do' },
        { n: 'sie-definition-manager', via: 'Definitions and Ascriptions authored' },
        { n: 'sie-operator', via: 'in-effect constraints and Mechanisms' },
        { n: 'itip-web-application', todo: true, via: 'execution history, appraisal measures and findings' },
        { n: 'Governance', side: 'human', via: 'compliance, impact, and audit evidence' },
      ],
      feedback: 'Evidence returns to intent — this is the corrective path that makes regulation and disposition a loop rather than two one-way declarations.',
      organisation: ['A definition of itself that is typed, attributable, and current, rather than tacit and dispersed across documents and people', 'Accountability discharged with evidence — appraisal, simulation, and audit of both AI and human action'],
      poesis: ['The closing return path that makes the governance loop corrective rather than declarative', 'Appraisal and audit assets that stay meaningful as models and workloads change'],
      comps: 'Authoring, lifecycle history, and appraisal Mechanisms in <b>sie-definition-manager</b> and <b>sie-operator</b>; the <b>itip-web-application</b> appraisal, simulation, and audit surfaces remain to be built.',
    },
  ];

  const svg = document.getElementById('vf-wires');
  const diagram = document.getElementById('vf-diagram');
  const detail = document.getElementById('vf-detail');
  let selectedFlow = null;
  let selectedComponent = null;

  function anchor(el, side) {
    const r = el.getBoundingClientRect();
    const d = diagram.getBoundingClientRect();
    return { x: (side === 'right' ? r.right : r.left) - d.left, y: r.top + r.height / 2 - d.top };
  }

  function anchorBottom(el) {
    const r = el.getBoundingClientRect();
    const d = diagram.getBoundingClientRect();
    return { x: r.left + r.width / 2 - d.left, y: r.bottom - d.top };
  }

  function draw() {
    if (!svg || !diagram) return;
    svg.innerHTML = '';
    if (window.innerWidth <= 860) return;
    svg.setAttribute('width', String(diagram.clientWidth));
    svg.setAttribute('height', String(diagram.clientHeight));

    const dRect = diagram.getBoundingClientRect();
    const colsEl = diagram.querySelector('.vf-cols');
    const gutterEl = diagram.querySelector('.vf-gutter');
    const railBase = colsEl ? colsEl.getBoundingClientRect().top - dRect.top - 20 : 0;
    const gutterRect = gutterEl ? gutterEl.getBoundingClientRect() : null;
    const gutterX = gutterRect ? gutterRect.left + gutterRect.width / 2 - dRect.left : 0;

    const ordered = FLOWS.filter((f) => f.loop).concat(FLOWS.filter((f) => !f.loop));

    ordered.forEach((f) => {
      const fromEl = document.getElementById(f.from);
      const toEl = document.getElementById(f.to);
      if (!fromEl || !toEl) return;
      const related = selectedComponent && (f.from === selectedComponent || f.to === selectedComponent);
      const sel = selectedFlow === f.id || related;
      const dim = (selectedFlow || selectedComponent) && !sel;

      let pathData;
      let tips;
      if (f.loop) {
        const orgFirst = f.from.indexOf('n-org') === 0;
        const o = anchorBottom(orgFirst ? fromEl : toEl);
        const s = anchor(orgFirst ? toEl : fromEl, 'left');
        const railY = railBase + (f.rail || 0);
        const gx = gutterX + (f.lane || 0);
        pathData = orgFirst
          ? `M ${o.x} ${o.y} V ${railY} H ${gx} V ${s.y} H ${s.x}`
          : `M ${s.x} ${s.y} H ${gx} V ${railY} H ${o.x} V ${o.y}`;
        tips = f.both ? [o, s] : [orgFirst ? s : o];
      } else {
        const a = anchor(fromEl, fromEl.closest('#g-partner') ? 'right' : 'left');
        const b = anchor(toEl, toEl.closest('#g-partner') ? 'right' : 'left');
        const midX = (a.x + b.x) / 2;
        const lane = f.lane ?? 0;
        pathData = `M ${a.x} ${a.y} C ${midX} ${a.y + lane}, ${midX} ${b.y + lane}, ${b.x} ${b.y}`;
        tips = [b];
      }

      const hit = document.createElementNS('http://www.w3.org/2000/svg', 'path');
      hit.setAttribute('d', pathData);
      hit.setAttribute('fill', 'none');
      hit.setAttribute('stroke', 'transparent');
      hit.setAttribute('stroke-width', '20');
      hit.setAttribute('class', 'vf-flow-hit');
      hit.setAttribute('aria-label', f.title);
      hit.addEventListener('click', () => selectFlow(f.id));
      svg.appendChild(hit);

      const p = document.createElementNS('http://www.w3.org/2000/svg', 'path');
      p.setAttribute('d', pathData);
      p.setAttribute('fill', 'none');
      p.setAttribute('stroke', f.hex);
      p.setAttribute('pointer-events', 'none');
      if (f.loop) {
        p.setAttribute('class', 'vf-flow-loop');
        p.setAttribute('stroke-width', sel ? '5' : '3');
        p.setAttribute('stroke-dasharray', '1 7');
        p.setAttribute('opacity', dim ? '0.14' : sel ? '0.95' : '0.5');
      } else {
        p.setAttribute('stroke-width', sel ? '7' : '5');
        p.setAttribute('opacity', dim ? '0.16' : '0.82');
        if (f.dashed) p.setAttribute('stroke-dasharray', '6 5');
      }
      svg.appendChild(p);

      tips.forEach((point) => {
        const tip = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
        tip.setAttribute('cx', String(point.x));
        tip.setAttribute('cy', String(point.y));
        tip.setAttribute('r', f.loop ? (sel ? '5' : '4') : sel ? '6' : '5');
        tip.setAttribute('fill', f.hex);
        tip.setAttribute('opacity', dim ? '0.16' : f.loop && !sel ? '0.6' : '1');
        tip.setAttribute('pointer-events', 'none');
        svg.appendChild(tip);
      });
    });
  }

  function schemaGraph(flow) {
    const nodes = [];
    const indexByName = new Map();
    flow.schema.forEach((entry) => {
      if (!indexByName.has(entry.n)) {
        indexByName.set(entry.n, nodes.length);
        nodes.push({ n: entry.n, side: entry.side, todo: Boolean(entry.todo) });
      } else if (entry.todo) {
        nodes[indexByName.get(entry.n)].todo = true;
      }
    });
    const edges = [];
    for (let i = 1; i < flow.schema.length; i += 1) {
      edges.push({
        from: indexByName.get(flow.schema[i - 1].n),
        to: indexByName.get(flow.schema[i].n),
        label: flow.schema[i].via || '',
      });
    }
    return { nodes, edges };
  }

  function schemaMarkup(flow) {
    if (!flow.schema || !flow.schema.length) return '';
    const { nodes, edges } = schemaGraph(flow);
    const boxes = nodes
      .map((node, index) => {
        const side = node.side;
        const kind = side === 'llm' || side === 'human' || side === 'material' ? side : 'poesis';
        const todo = node.todo ? '<span class="vf-node-todo">planned</span>' : '';
        return `<span class="vf-node vf-node-${kind}" data-n="${index}">${node.n}${todo}</span>`;
      })
      .join('');
    const steps = edges
      .map((edge, index) => `<li><span class="vf-step-num">${index + 1}</span><span>${edge.label || '&mdash;'}</span></li>`)
      .join('');
    const feedback = flow.feedback
      ? `<p class="vf-schema-feedback">&#8617; ${flow.feedback}</p>`
      : '';
    return `<div class="vf-schema">
      <p class="vf-schema-legend"><span class="vf-node vf-node-llm">LLM operation</span><span class="vf-node vf-node-poesis">Poesis stack component</span><span class="vf-node vf-node-human">organisation operation</span><span class="vf-node vf-node-material">material</span></p>
      <div class="vf-schema-body">
        <div class="vf-schema-graph" data-schema-flow="${flow.id}" role="img" aria-label="Flow: ${flow.schema.map((node) => node.n).join(' to ')}">
          <svg class="vf-schema-wires" aria-hidden="true"></svg>
          <div class="vf-schema-nodes">${boxes}</div>
        </div>
        <ol class="vf-schema-steps">${steps}</ol>
      </div>
      ${feedback}
    </div>`;
  }

  function drawSchemas() {
    document.querySelectorAll('.vf-schema-graph[data-schema-flow]').forEach((graph) => {
      const flow = FLOWS.find((candidate) => candidate.id === graph.dataset.schemaFlow);
      const wires = graph.querySelector('.vf-schema-wires');
      if (!flow || !wires) return;
      const boxes = Array.from(graph.querySelectorAll('.vf-node[data-n]'));
      if (!boxes.length) return;
      const gRect = graph.getBoundingClientRect();
      wires.setAttribute('width', String(graph.clientWidth));
      wires.setAttribute('height', String(graph.clientHeight));
      const box = (index) => {
        const r = boxes[index].getBoundingClientRect();
        return {
          left: r.left - gRect.left,
          right: r.right - gRect.left,
          top: r.top - gRect.top,
          bottom: r.bottom - gRect.top,
          midY: r.top + r.height / 2 - gRect.top,
        };
      };
      const stackLeft = box(0).left;
      const maxRight = boxes.reduce(
        (max, el) => Math.max(max, el.getBoundingClientRect().right - gRect.left),
        0,
      );
      const wire = (d) => `<path d="${d}" fill="none" stroke-width="1.5" style="stroke: var(--muted)" opacity="0.85"/>`;
      const head = (d) => `<path d="${d}" style="fill: var(--muted)" opacity="0.85"/>`;
      const badge = (x, y, n) =>
        `<circle cx="${x}" cy="${y}" r="7.5" style="fill: var(--bg-soft); stroke: var(--line-strong)"/>` +
        `<text x="${x}" y="${y + 3}" text-anchor="middle" style="fill: var(--ink); font: 600 9px var(--font-mono)">${n}</text>`;
      let leftLanes = 0;
      let rightLanes = 0;
      const usedAdjacent = new Set();
      let markup = '';
      schemaGraph(flow).edges.forEach((edge, index) => {
        const a = box(edge.from);
        const b = box(edge.to);
        const num = index + 1;
        if (edge.to === edge.from + 1 && !usedAdjacent.has(`${edge.from}-${edge.to}`)) {
          usedAdjacent.add(`${edge.from}-${edge.to}`);
          const x = stackLeft + 16;
          markup += wire(`M ${x} ${a.bottom} V ${b.top - 5}`);
          markup += head(`M ${x} ${b.top - 1} L ${x - 3.5} ${b.top - 7} L ${x + 3.5} ${b.top - 7} Z`);
          markup += badge(x + 17, (a.bottom + b.top) / 2, num);
        } else if (edge.to > edge.from) {
          leftLanes += 1;
          const lane = stackLeft - 10 - leftLanes * 9;
          markup += wire(`M ${a.left} ${a.midY} H ${lane} V ${b.midY} H ${b.left - 6}`);
          markup += head(`M ${b.left - 1} ${b.midY} L ${b.left - 7} ${b.midY - 3.5} L ${b.left - 7} ${b.midY + 3.5} Z`);
          markup += badge(lane, (a.midY + b.midY) / 2, num);
        } else {
          rightLanes += 1;
          const lane = maxRight + 10 + rightLanes * 9;
          markup += wire(`M ${a.right} ${a.midY} H ${lane} V ${b.midY} H ${b.right + 6}`);
          markup += head(`M ${b.right + 1} ${b.midY} L ${b.right + 7} ${b.midY - 3.5} L ${b.right + 7} ${b.midY + 3.5} Z`);
          markup += badge(lane, (a.midY + b.midY) / 2, num);
        }
      });
      wires.innerHTML = markup;
    });
  }

  function adherenceMarkup(flow) {
    const shared = flow.shared && flow.shared.length
      ? `<div class="vf-shared">
        <h5>Shared value in the customer organisation</h5>
        <ul>${flow.shared.map((value) => `<li>${value}</li>`).join('')}</ul>
      </div>`
      : '';
    return `<article class="vf-adherence">
      <h4 style="color:${flow.hex}">${flow.title}</h4>
      <p class="vf-dsub">${flow.sub}</p>
      ${schemaMarkup(flow)}
      <div class="vf-dcols">
        <div><h5>${flow.loop ? 'Value for the customer organisation' : 'Value for the LLM vendor partner'}</h5><ul>${(flow.loop ? flow.organisation : flow.partner).map((value) => `<li>${value}</li>`).join('')}</ul></div>
        <div><h5>Value for Poesis</h5><ul>${flow.poesis.map((value) => `<li>${value}</li>`).join('')}</ul></div>
      </div>
      ${shared}
      <div class="vf-comps">Adherence locus — ${flow.comps}</div>
      <p class="vf-maturity">${flow.maturity}</p>
    </article>`;
  }

  function showAdherences(title, flows) {
    if (!detail) return;
    detail.hidden = false;
    detail.innerHTML = `<h3>${title}</h3>${flows.map(adherenceMarkup).join('')}`;
    draw();
    drawSchemas();
    detail.focus({ preventScroll: true });
    detail.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }

  function clearSelection() {
    selectedFlow = null;
    selectedComponent = null;
    document.querySelectorAll('.vf-card').forEach((card) => card.classList.remove('vf-selected'));
    document.querySelectorAll('.vf-card-select').forEach((control) => control.setAttribute('aria-pressed', 'false'));
    document.querySelectorAll('.value-card[data-flow], .vf-mobile-flow[data-flow]').forEach((control) => control.setAttribute('aria-pressed', 'false'));
    if (detail) detail.hidden = true;
    draw();
  }

  function selectFlow(id) {
    if (selectedFlow === id && !selectedComponent) {
      clearSelection();
      return;
    }
    selectedFlow = id;
    selectedComponent = null;
    document.querySelectorAll('.vf-card').forEach((card) => card.classList.remove('vf-selected'));
    document.querySelectorAll('.vf-card-select').forEach((control) => control.setAttribute('aria-pressed', 'false'));
    document.querySelectorAll('.value-card[data-flow], .vf-mobile-flow[data-flow]').forEach((control) => {
      control.setAttribute('aria-pressed', String(control.dataset.flow === id));
    });
    const flow = FLOWS.find((candidate) => candidate.id === id);
    if (!flow) {
      clearSelection();
      return;
    }
    showAdherences(`Selected value flow — ${flow.label}`, [flow]);
  }

  function selectComponent(card) {
    if (selectedComponent === card.id && !selectedFlow) {
      clearSelection();
      return;
    }
    selectedComponent = card.id;
    selectedFlow = null;
    document.querySelectorAll('.value-card[data-flow], .vf-mobile-flow[data-flow]').forEach((control) => control.setAttribute('aria-pressed', 'false'));
    document.querySelectorAll('.vf-card').forEach((candidate) => {
      const selected = candidate === card;
      candidate.classList.toggle('vf-selected', selected);
      candidate.querySelector('.vf-card-select')?.setAttribute('aria-pressed', String(selected));
    });
    const op = card.dataset.op;
    const flows = FLOWS.filter((flow) => {
      if (flow.from === card.id || flow.to === card.id) return true;
      if (!op || !flow.schema) return false;
      return flow.schema.some((node) => node.side === 'human' && node.n === op);
    });
    showAdherences(`${card.dataset.component} — ${flows.length} connected adherence${flows.length === 1 ? '' : 's'}`, flows);
  }

  document.querySelectorAll('.vf-card[data-component]').forEach((card) => {
    card.querySelector('.vf-card-select')?.addEventListener('click', () => selectComponent(card));
  });

  document.querySelectorAll('.value-card[data-flow], .vf-mobile-flow[data-flow], .vf-pill[data-flow]').forEach((control) => {
    control.addEventListener('click', () => selectFlow(control.dataset.flow));
  });

  document.querySelectorAll('.vf-fold-btn').forEach((btn) => {
    btn.addEventListener('click', (event) => {
      event.stopPropagation();
      const fold = document.getElementById(btn.dataset.fold);
      if (!fold) return;
      const expanded = !fold.classList.contains('open');
      fold.classList.toggle('open', expanded);
      fold.hidden = !expanded;
      btn.setAttribute('aria-expanded', String(expanded));
      btn.textContent = `${expanded ? '▾' : '▸'} components`;
      draw();
    });
  });

  window.addEventListener('resize', () => {
    draw();
    drawSchemas();
  });
  draw();
</script>
