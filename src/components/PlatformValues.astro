---
/**
 * PlatformValues — platform benefits composed from explicit use cases.
 */
import CatalogCard from './CatalogCard.astro';
import { platformValues } from '../data/usage.ts';
import { valueCard } from '../data/catalog-card.ts';
---
<ul class="think-consequence-list catalog-grid">
  {platformValues.map((value) => <CatalogCard card={valueCard(value)} />)}
</ul>
